spring:
  cloud:
    config:
      discovery:
        service-id: config-center
        enabled: true               #必须有，声明配置服务器是可发现的
      name: test
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:50001/eureka/,http://eureka2:50002/eureka/

